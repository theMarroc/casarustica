/** Todo el taller usa el acento tiffany, dentro del mismo sitio. */
export default function LayoutTaller({ children }: { children: React.ReactNode }) {
  return <div className="marca-taller">{children}</div>;
}
